services:
  - type: web
    name: advanced-coal-mine-digital-twin
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m backend.server
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
